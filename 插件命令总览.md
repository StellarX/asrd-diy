# AS:RD 插件集命令总览（完整版）

> 项目：`D:\project\asrddiy` ｜ 游戏：Alien Swarm: Reactive Drop (AppID 563560)
>
> **使用说明**：
>
> - 命令分为「玩家命令」「管理员命令」「ConVar 设置」三类。
>
> - 设置 ConVar 必须带 `sm_cvar` 前缀，例如 `sm_cvar sm_asrd_sentry_range_mult 2.0`。
>
> - 下文代码块中 `#` 及其后的文字是**注释**，复制时只复制命令部分（`#` 之前）。
>
> - 所有管理员命令需 `ADMFLAG_GENERIC`（通用管理员权限）。

***

## 通用管理命令（SourceMod，不限插件）

```bash
sm plugins list                          # 列出所有插件及版本号
sm plugins info asrd_alien_civilwar      # 查看单个插件详情（换成对应插件名）
sm plugins reload asrd_alien_civilwar    # 重新加载插件（替换 .smx 后热更新）
sm plugins unload asrd_alien_civilwar    # 卸载插件（临时禁用/排查崩溃）
sm plugins load asrd_alien_civilwar      # 加载插件
sm plugins refresh                       # 重载所有插件
sm_cvar sm_asrd_betray_version           # 查询叛变虫群插件版本号
```

***

## 一、陆战队员强化（Marine Power）v1.2.0

按键实时调大/调小自己的血量、体型、近战、移速。

### 玩家命令（默认公开）

```bash
sm_power_up        # 调大 1 级（血量 +400/级、体型 +0.2/级、移速同步、近战放大）
sm_power_down      # 调小 1 级（仅模型缩小 0.8/0.6/0.4 倍）
sm_power_reset     # 恢复默认（等级 0 = 100 血 / 1.0 体型 / 1.0 移速）
bind F6 "sm_power_up"      # 绑定示例（控制台输入一次，自动存 config.cfg）
bind F7 "sm_power_down"
```

### 管理员命令

```bash
sm_power_status    # 查看所有玩家强化状态
sm_power_set <玩家> <等级>   # 指定强化某玩家 (等级= -缩小max~放大max, 0=恢复默认; 玩家可填名字/部分名/#userid)
```

### ConVar 设置

```bash
sm_cvar sm_asrd_power_enabled 1         # 总开关 (0=关 1=开)
sm_cvar sm_asrd_power_public 1          # 允许普通玩家自行强化 (0=仅管理员 1=公开)
sm_cvar sm_asrd_power_max_level 5       # 放大最大等级 (1~10)
sm_cvar sm_asrd_power_hp_step 400       # 每级血量增量 (5级=2000)
sm_cvar sm_asrd_power_scale_step 0.2    # 每级体型增量 (5级=x2.0)
sm_cvar sm_asrd_power_shrink_max 3      # 缩小最大等级 (1~5, 仅缩模型)
sm_cvar sm_asrd_power_shrink_step 0.2   # 每级缩小比例 (3级=x0.4)
sm_cvar sm_asrd_power_speed_enabled 1   # 放大时同步提高移速 (0/1)
sm_cvar sm_asrd_power_speed_step 0.2    # 每级移速增量
sm_cvar sm_asrd_power_melee_enabled 1   # 启用全局近战放大 (0/1, 影响所有队员)
sm_cvar sm_asrd_power_debug 0           # 调试输出 (0/1)
```

***

## 二、哨戒塔增强 + 头顶塔 + HUD（Sentry Enhancer）v6.3.0

增强地图哨戒塔，支持把塔放头顶当随行炮台。

### 管理员命令

```bash
sm_sentry_refresh           # 重新增强所有哨戒塔并补满弹药
sm_sentry_status            # 查看所有哨戒塔状态
sm_sentry_boost             # 一键满配增强所有哨戒塔
sm_sentry_unboost           # 一键还原增强倍率到默认档
sm_sentryhat 90             # 把最近的塔放到自己头顶（90=朝向偏移度数，可省略）
sm_sentryhat_off            # 取消所有玩家的头顶塔
sm_sentry_drop 1            # 在身边掉落哨戒塔箱（0机枪 1炮 2喷火 3冰冻，默认炮）
sm_sentry_dump              # 转储哨戒塔内部属性（调试）
sm_sentry_dump_player       # 转储玩家实体属性（调试）
```

### 玩家命令

```bash
sm_hat 90            # 把最近的塔放到自己头顶（需 hat_public=1，默认开）
sm_hat_off           # 取消自己的头顶塔
sm_sentryhud         # 开关右上角哨戒塔信息 HUD
sm_sentrydrop 1      # 掉落哨戒塔箱（需 drop_public=1，默认关）
```

### ConVar 设置（可单独微调每一项）

```bash
sm_cvar sm_asrd_sentry_enabled 1          # 总开关 (0=关 1=开)
sm_cvar sm_asrd_sentry_health_mult 2.0    # 生命倍率 (1.0=默认)
sm_cvar sm_asrd_sentry_firerate_mult 35   # 射速倍率 (1.0=默认)
sm_cvar sm_asrd_sentry_range_mult 1.5     # 射程倍率 ← 单独改射程就改这个
sm_cvar sm_asrd_sentry_ammo_mult 100      # 弹药倍率 (1.0=默认)
sm_cvar sm_asrd_sentry_damage_mult 1.0    # 子弹伤害倍率 (机枪10/炮60/喷火4, 冰冻塔不增强)
sm_cvar sm_asrd_sentry_invulnerable 0     # 无敌 (0=正常 1=不会死)
sm_cvar sm_asrd_sentry_no_player_damage 1 # 关闭误伤队友 (0=可伤 1=不伤)
sm_cvar sm_asrd_sentry_hat_public 1       # 允许所有玩家用头顶塔命令 (0=仅管理员)
sm_cvar sm_asrd_sentry_hat_turnspeed 360  # 头顶塔转向速度 度/秒 (0=瞬间)
sm_cvar sm_asrd_sentry_hat_maxdist 100    # 头顶塔命令最大距离 (0=不限)
sm_cvar sm_asrd_sentry_hat_layerspace 60  # 头顶多座塔的层间距 (20~200)
sm_cvar sm_asrd_sentry_turn_rate 0        # 塔顶转向速度 度/秒 (0=瞬间, 150=引擎默认)
sm_cvar sm_asrd_sentry_hud_default 0      # 新玩家默认开启 HUD (0/1)
sm_cvar sm_asrd_sentry_drop_limit 20      # 场上最多可存在的拾取箱数 (0=不限)
sm_cvar sm_asrd_sentry_drop_public 0      # 允许所有玩家用掉落命令 (0=仅管理员)
sm_cvar sm_asrd_sentry_debug 0            # 调试输出 (0/1)
```

***

## 三、核弹轰炸（Nuke Strike）v1.7.1

倒计时后全图清除虫族（含异形卵）。

### 管理员命令

```bash
sm_nuke      # 呼叫战术核弹轰炸全图虫族
```

### 玩家命令

```bash
sm_nukepub   # 呼叫核弹（需 nuke_public=1，默认关）
```

### ConVar 设置

```bash
sm_cvar sm_asrd_nuke_enabled 1        # 总开关 (0=关 1=开)
sm_cvar sm_asrd_nuke_damage 999999.0  # 对虫族伤害
sm_cvar sm_asrd_nuke_delay 5.0        # 倒计时秒数 (0=立即引爆)
sm_cvar sm_asrd_nuke_public 0         # 允许玩家用 sm_nukepub (0=仅管理员 1=所有人)
sm_cvar sm_asrd_nuke_debug 0          # 调试输出 (0/1)
```

***

## 四、实体趣味操控（Entity Fun）v1.0.0

对实体做传送、父子绑定、穿墙、冻结、弹射等操控。**全部仅管理员，无玩家命令，无 ConVar。**

```bash
sm_sentryhat target            # 把机枪塔放到目标玩家头顶（target=玩家名或 #userid）
sm_sentryhat_off               # 取消所有头顶机枪塔
sm_stack target1 target2       # 把 target1 叠到 target2 头顶
sm_noclip_marine target        # 让目标陆战队员穿墙飞行
sm_freeze_marine target        # 冻结/解冻目标陆战队员
sm_launch target               # 把目标向上弹射
sm_slam target                 # 把目标砸向地面
```

***

## 五、范围击退（Repulse）v1.2.1

按一个键，以自己为中心震开周围所有虫族（可连按，默认无冷却）。击退采用每游戏帧平滑推进并附带速度，短时也不顿挫；另支持"持续斥力护盾"自动推开靠近的虫。

### 玩家命令（默认公开）

```bash
sm_repulse        # 以自己为中心震开周围虫族
bind F9 "sm_repulse"      # 绑定示例（控制台输入一次，自动存 config.cfg）
```

### ConVar 设置

```bash
sm_cvar sm_asrd_repulse_enabled 1        # 总开关 (0=关 1=开)
sm_cvar sm_asrd_repulse_public 1         # 允许普通玩家使用 (0=仅管理员)
sm_cvar sm_asrd_repulse_radius 400       # 手动击退作用半径 (游戏单位)
sm_cvar sm_asrd_repulse_force 160        # 单次击退向外的总位移 (游戏单位)
sm_cvar sm_asrd_repulse_lift 80          # 单次击退向上挑飞高度
sm_cvar sm_asrd_repulse_pull_time 0.35   # 单次击退推进时长/秒 (越大越慢越平滑)
sm_cvar sm_asrd_repulse_cooldown 0       # 手动触发间隔秒 (0=无冷却可连按)

# 持续斥力护盾 (自动推开靠近的虫)
sm_cvar sm_asrd_repulse_aura 1           # 护盾开关 (0=关 1=开)
sm_cvar sm_asrd_repulse_aura_radius 260  # 护盾半径 (游戏单位)
sm_cvar sm_asrd_repulse_aura_speed 180   # 护盾外推速度/单位每秒 (越小越柔和)
sm_cvar sm_asrd_repulse_classes ""        # 追加要击退的实体类名(空格分隔)，如 "npc_harvester asw_mender"
sm_cvar sm_asrd_repulse_debug 0          # 调试输出 (0/1; 1 会列出半径内所有 asw_/npc_ 实体真实类名)
```

> **如果某只虫推不动：** 先开 debug（`sm_cvar sm_asrd_repulse_debug 1`）按一次键，服务器控制台会打印半径内所有 `asw_`/`npc_` 实体的真实类名，把漏掉的那个类名填进 `sm_asrd_repulse_classes` 即可，无需改代码重编译。蚁狮守卫/工蜂等使用 `npc_antlionguard*` / `npc_antlion_worker` 类名，已内置。

***

## 六、电锯高速旋转（Chainsaw Turbo）v1.2.0

手持电锯按住攻击键时锯片转得更快（被动生效，无需命令触发）。

### 管理员命令

```bash
sm_chainsaw_status   # 查看当前谁手持电锯、是否开火等状态
```

### ConVar 设置

```bash
sm_cvar sm_asrd_chainsaw_enabled 1    # 总开关 (0=关 1=开)
sm_cvar sm_asrd_chainsaw_speed 3.0    # 锯片转速倍率 (1.0~12.0, 3.0=快3倍)
sm_cvar sm_asrd_chainsaw_public 0     # 对普通玩家生效 (0=仅管理员 1=所有人)
sm_cvar sm_asrd_chainsaw_debug 0      # 调试输出 (0/1)
```

***

## 七、叛变虫群（Alien Civil War）v4.6.0

生成一批攻击虫族（而非玩家）的叛变虫，外观着色区分（默认绿色），可自选虫种、免冷却。

### 管理员命令

```bash
sm_betray drone 10      # 生成叛变虫（type=虫种, count=数量, 默认10）
sm_betray drone 10 jack # 生成到玩家 jack 身旁（可选第3参数 target, 可填名字/部分名/#userid）
sm_betray_list          # 列出可生成的虫种
sm_betray_clear         # 清除本插件生成的所有叛变虫
```

### 玩家命令

```bash
sm_betraypub drone 10   # 生成叛变虫（需 betray_public=1，默认关）
bind F8 "sm_betraypub"  # 绑定示例
```

### ConVar 设置

```bash
sm_cvar sm_asrd_betray_enabled 1        # 总开关 (0=关 1=开)
sm_cvar sm_asrd_betray_type asw_drone   # 默认生成的虫种类名
sm_cvar sm_asrd_betray_count 10         # 默认每批生成数量 (上限 80)
sm_cvar sm_asrd_betray_spread 120.0     # 生成散布半径
sm_cvar sm_asrd_betray_color "37 217 73" # 叛变虫着色 RGB（默认绿色）
sm_cvar sm_asrd_betray_public 0         # 允许玩家用 sm_betraypub (0=仅管理员 1=所有人)
sm_cvar sm_asrd_betray_drone_scale 1.0  # 叛变 drone 体型缩放倍率（仅 asw_drone, 1.0=默认）
sm_cvar sm_asrd_betray_damage_mult 4.0  # 叛变虫攻击力倍率（仅叛变虫造成的伤害, 1.0=不增强）
sm_cvar sm_asrd_betray_health_mult 5.0  # 叛变虫血量倍率（仅叛变虫, 默认 5 倍）
sm_cvar sm_asrd_betray_speed_mult 2.0   # 叛变虫移动速度倍率（仅叛变虫, 1.0=默认速度）
sm_cvar sm_asrd_betray_anim_mult 2.0    # 叛变虫动画速度倍率（仅叛变虫, 1.0=默认动画速度）
sm_cvar sm_asrd_betray_debug 0          # 调试输出 (0/1)
```

### 可生成虫种（sm\_betray 的 type 参数）

| 类名                      | 中文名   | 别名          |
| ----------------------- | ----- | ----------- |
| `asw_drone`             | 普通工蜂  | `drone`     |
| `asw_drone_jumper`      | 跳跃工蜂  | `jumper`    |
| `asw_buzzer`            | 蜂群    | `buzzer`    |
| `asw_parasite`          | 抱脸寄生虫 | `parasite`  |
| `asw_parasite_defanged` | 拔牙寄生虫 | `defanged`  |
| `asw_boomer`            | 爆裂虫   | `boomer`    |
| `asw_ranger`            | 游侠    | `ranger`    |
| `asw_shieldbug`         | 盾甲虫   | `shield`    |
| `asw_mortarbug`         | 迫击炮虫  | `mortar`    |
| `asw_harvester`         | 收割者   | `harvester` |
| `asw_grub`              | 幼虫    | `grub`      |

***

## 八、重生传送（Respawn & Teleport）v1.0.0

阵亡后 `/fh` 重生（每局每人一次），存活时 `/tp` 传送到最近存活队友旁。**默认关闭，需管理员激活。**

### 管理员命令

```bash
sm_fhtp_enable        # 切换开关 (不带参数)
sm_fhtp_enable 1      # 开启
sm_fhtp_enable 0      # 关闭
```

### 玩家命令

```bash
/fh  # 阵亡后重生（每局一次，落点在被观战玩家旁）
/tp  # 传送到最近的存活队友身边（仅存活时可用）
```

### ConVar 设置

```bash
sm_cvar sm_asrd_fhtp_enabled 1    # 总开关 (0=关 1=开, 默认 0)
sm_cvar sm_asrd_fhtp_debug 0      # 调试输出 (0/1)
```

### ⚠️ 启用 /fh 前必读（SDKCall 偏移配置）

`/fh` 需调用引擎虚拟函数 `CASW_Player::ResurrectMarine`。其虚表索引存放在 gamedata：

```
addons/sourcemod/gamedata/asrd_respawn.games.txt
  Games -> reactivedrop -> Offsets -> "ResurrectMarine" = <虚表索引>
```

当前为占位值 `0`（未配置），此时 `/fh` 会安全拒绝、不崩服。**请先通过游戏二进制符号/反汇编确定正确虚表索引填入该文件，再** **`sm plugins reload asrd_respawn`**。AS:RD 升级后索引可能变化，需重新核对。

> `/tp` 纯实体操作，不依赖 SDKCall，可独立正常使用。

***

## 九、一键开锁（Auto Unlock）v1.0.3

破解游戏内门锁 mini-game（连线拼图/转盘破解），直接解锁上锁的按钮区（大门）。

### 玩家命令（默认公开）

```bash
sm_unlock                # 解锁以你为中心 unlock_range 内最近的一扇上锁门锁
bind F9 "sm_unlock"      # 绑定示例
```

> `sm_unlock` 会解锁以你为中心 `unlock_range`（默认 150 单位，很小）内最近的一扇上锁门锁，必须贴近门才生效，不做大范围自动解锁。

### 管理员命令

```bash
sm_unlock_all            # 解锁整张地图所有上锁按钮区
sm_unlock_status         # 列出场上按钮区锁/电状态（排查用）
```

### ConVar 设置

```bash
sm_cvar sm_asrd_unlock_enabled 1     # 总开关 (0=关 1=开)
sm_cvar sm_asrd_unlock_public 1      # 允许普通玩家用 sm_unlock (0=仅管理员 1=公开)
sm_cvar sm_asrd_unlock_range 150     # sm_unlock 附近解锁范围（世界单位，需贴近门锁才能解，0=不限）
sm_cvar sm_asrd_unlock_forcepower 1  # 断电的门先送 PowerOn 再解锁 (0=只解锁)
sm_cvar sm_asrd_unlock_debug 0       # 调试输出 (0=关 1=开)
```

### 原理与局限

- 门锁实体为 `trigger_asw_button_area`（引擎类 `CASW_Button_Area`），破解完成后引擎 `InputUnlock` 会把 `m_bIsLocked` 置 false、`m_fHackProgress` 置 1.0 来开门。本插件直接向目标发送引擎输入 `"Unlock"` 触发同一逻辑，从而绕过连线/转盘小游戏。

- 个别地图若把门开的剧情绑定在 `OnButtonHackCompleted` 输出点（而非仅依赖锁状态/`useafterhack`），`InputUnlock` 不会触发该输出点，门可能开了但剧情不推进——此时请改用 `sm_unlock_all` 或先测试确认。

***

## 十、AFK 自动踢人（AFK Kick）

检测挂机玩家，5 分钟无操作自动踢出（无玩家/管理员命令，纯被动检测）。

### ConVar 设置

```bash
sm_cvar sm_asrd_afk_enabled 1          # 启用/禁用插件 (0=禁用 1=启用)
sm_cvar sm_asrd_afk_kick_time 300      # 挂机多少秒后踢出 (默认300=5分钟)
sm_cvar sm_asrd_afk_warn_time 240      # 挂机多少秒后警告 (默认240=4分钟)
sm_cvar sm_asrd_afk_check_interval 10  # 检测间隔秒数 (默认10秒)
```

> 检测依据：WASD 移动、鼠标按键（开火/技能/换弹）、切换武器/使用物品。**不检测聊天消息**；拥有 `kick` 权限（ADMFLAG\_KICK）的管理员不会被踢。

***

## 十一、欢迎/告别消息（Welcome）

玩家进出服务器时广播打招呼语（无玩家/管理员命令，纯被动广播）。

### ConVar 设置

```bash
sm_cvar sm_asrd_welcome_enabled 1      # 启用/禁用插件
sm_cvar sm_asrd_welcome_msg "欢迎 {player} 加入服务器！当前在线: {count} 人"  # 加入广播消息
sm_cvar sm_asrd_goodbye_msg "{player} 离开了服务器，当前在线: {count} 人"  # 离开广播消息
sm_cvar sm_asrd_welcome_personal 1     # 是否向加入者发送专属欢迎信息
sm_cvar sm_asrd_show_country 0         # 是否显示玩家国家/地区 (需 geoip 扩展)
sm_cvar sm_asrd_welcome_sound ""       # 加入音效路径 (留空不播放)
```

**支持的变量:** `{player}` 玩家名称、`{count}` 当前在线人数、`{country}` 玩家国家/地区（需启用 `sm_asrd_show_country`）。

**自定义消息示例:**

```bash
sm_cvar sm_asrd_welcome_msg "🎮 {player} 上线了！服务器现有 {count} 人"
sm_cvar sm_asrd_goodbye_msg "👋 {player} 溜了，还剩 {count} 个战友"
sm_cvar sm_asrd_welcome_sound "ui/menu_enter.wav"
```

