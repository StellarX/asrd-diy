> 项目：`d:\game\asrd-plugins` ｜ 游戏：Alien Swarm: Reactive Drop (AppID 563560)
>
> **使用说明**：
>
> - 命令分为「玩家命令」「管理员命令」「ConVar 设置」三类。
> - 设置 ConVar 必须带 `sm_cvar` 前缀，例如 `sm_cvar sm_asrd_sentry_range_mult 2.0`。
> - 下文代码块中 `#` 及其后的文字是**注释**，复制时只复制命令部分（`#` 之前）。
> - 所有管理员命令需 `ADMFLAG_GENERIC`（通用管理员权限）。
> - **积分购买统一走聊天框**（`/buy`、`/1`、`/2`、`/3`），控制台无任何 `sm_points*` 命令，控制台调用原功能命令也不扣积分。



---

## 通用管理命令（SourceMod，不限插件）

```bash
sm plugins list                          # 列出所有插件及版本号
sm plugins info asrd_alien_civilwar      # 查看单个插件详情（换成对应插件名）
sm plugins reload asrd_alien_civilwar    # 重新加载插件（替换 .smx 后热更新）
sm plugins unload asrd_alien_civilwar    # 卸载插件（临时禁用/排查崩溃）
sm plugins load asrd_alien_civilwar      # 加载插件
sm plugins refresh                       # 重载所有插件
sm_cvar sm_asrd_betray_version           # 查询叛变虫群插件版本号
sm_cvar sm_asrd_points_version           # 查询积分机制插件版本号
```

---

## 一、陆战队员强化（Marine Power）v1.2.1

按键实时调大/调小自己的血量、体型、近战、移速。

### 玩家命令（默认公开）

```bash
sm_power_up        # 调大 1 级（血量按等级固定 L1=200/L2=300/L3=500/L4=800/L5=1000、体型 +0.1/级、移速同步、近战放大）
sm_power_down      # 调小 1 级（仅模型缩小 0.8/0.6/0.4 倍）
sm_power_reset     # 恢复默认（等级 0 = 100 血 / 1.0 体型 / 1.0 移速）
bind F6 "sm_power_up"      # 绑定示例（控制台输入一次，自动存 config.cfg）
bind F7 "sm_power_down"
```

### 管理员命令

```bash
sm_power_status    # 查看所有玩家强化状态
sm_power_set <玩家> <等级>   # 指定强化某玩家 (等级= -缩小max~放大max, 0=恢复默认; 玩家可填名字/部分名/#userid)
```

### ConVar 设置

```bash
sm_cvar sm_asrd_power_enabled 1         # 总开关 (0=关 1=开)
sm_cvar sm_asrd_power_public 1          # 允许普通玩家自行强化 (0=仅管理员 1=公开)
sm_cvar sm_asrd_power_max_level 5       # 放大最大等级 (1~10)
sm_cvar sm_asrd_power_scale_step 0.1    # 每级体型增量 (体型 = 1.0 + step×等级, 5级=x1.5)
sm_cvar sm_asrd_power_shrink_max 3      # 缩小最大等级 (1~5, 仅缩模型)
sm_cvar sm_asrd_power_shrink_step 0.2   # 每级缩小比例 (3级=x0.4)
sm_cvar sm_asrd_power_speed_enabled 1   # 放大时同步提高移速 (0/1)
sm_cvar sm_asrd_power_speed_step 0.1    # 每级移速增量 (与体型同比例, 5级=x1.5)
sm_cvar sm_asrd_power_melee_enabled 1   # 启用全局近战放大 (0/1, 影响所有队员)
sm_cvar sm_asrd_power_debug 0           # 调试输出 (0/1)

# 注: 血量无 ConVar, 由代码固定配置 L1=200/L2=300/L3=500/L4=800/L5=1000
# (等级超过 5 按 L5=1000 封顶); 满级后再购买强化见积分插件"满级加血"
```

---

## 二、哨戒塔增强 + 头顶塔 + HUD（Sentry Enhancer）v6.3.0

增强地图哨戒塔，支持把塔放头顶当随行炮台。

### 管理员命令

```bash
sm_sentry_refresh           # 重新增强所有哨戒塔并补满弹药
sm_sentry_status            # 查看所有哨戒塔状态
sm_sentry_boost             # 一键满配增强所有哨戒塔
sm_sentry_unboost           # 一键还原增强倍率到默认档
sm_sentryhat 90             # 把最近的塔放到自己头顶（90=朝向偏移度数，可省略）
sm_sentryhat_off            # 取消所有玩家的头顶塔
sm_sentry_drop 1            # 在身边掉落哨戒塔箱（0机枪 1炮 2喷火 3冰冻，默认炮）
sm_sentry_dump              # 转储哨戒塔内部属性（调试）
sm_sentry_dump_player       # 转储玩家实体属性（调试）
```

### 玩家命令

```bash
sm_hat 90            # 把最近的塔放到自己头顶（需 hat_public=1，默认开）
sm_hat_off           # 取消自己的头顶塔
sm_sentryhud         # 开关右上角哨戒塔信息 HUD
sm_sentrydrop 1      # 掉落哨戒塔箱（需 drop_public=1，默认关）
```


### ConVar 设置（可单独微调每一项）

```bash
sm_cvar sm_asrd_sentry_enabled 1          # 总开关 (0=关 1=开)
sm_cvar sm_asrd_sentry_health_mult 2.0    # 生命倍率 (1.0=默认)
sm_cvar sm_asrd_sentry_firerate_mult 35   # 射速倍率 (1.0=默认)
sm_cvar sm_asrd_sentry_range_mult 1.5     # 射程倍率 ← 单独改射程就改这个
sm_cvar sm_asrd_sentry_ammo_mult 100      # 弹药倍率 (1.0=默认)
sm_cvar sm_asrd_sentry_damage_mult 1.0    # 子弹伤害倍率 (机枪10/炮60/喷火4, 冰冻塔不增强)
sm_cvar sm_asrd_sentry_invulnerable 0     # 无敌 (0=正常 1=不会死)
sm_cvar sm_asrd_sentry_no_player_damage 1 # 关闭误伤队友 (0=可伤 1=不伤)
sm_cvar sm_asrd_sentry_hat_public 1       # 允许所有玩家用头顶塔命令 (0=仅管理员)
sm_cvar sm_asrd_sentry_hat_turnspeed 360  # 头顶塔转向速度 度/秒 (0=瞬间)
sm_cvar sm_asrd_sentry_hat_maxdist 100    # 头顶塔命令最大距离 (0=不限)
sm_cvar sm_asrd_sentry_hat_layerspace 60  # 头顶多座塔的层间距 (20~200)
sm_cvar sm_asrd_sentry_turn_rate 0        # 塔顶转向速度 度/秒 (0=瞬间, 150=引擎默认)
sm_cvar sm_asrd_sentry_hud_default 0      # 新玩家默认开启 HUD (0/1)
sm_cvar sm_asrd_sentry_drop_limit 20      # 场上最多可存在的拾取箱数 (0=不限)
sm_cvar sm_asrd_sentry_drop_public 0      # 允许所有玩家用掉落命令 (0=仅管理员)
sm_cvar sm_asrd_sentry_debug 0            # 调试输出 (0/1)
```

---

## 三、核弹轰炸（Nuke Strike）v1.7.7

倒计时后全图清除虫族（含异形卵）。倒计时由游戏帧回调（`OnGameFrame` + 时间戳）驱动，换图/重开不会残留定时器；管理员与玩家共用同一套倒计时 HUD（通道 5）。

### 管理员命令

```bash
sm_nuke      # 呼叫战术核弹轰炸全图虫族
```

### 玩家命令

```bash
sm_nukepub   # 呼叫核弹（需 nuke_public=1，默认开；聊天框 /buy 1 或 /1 同样触发并扣积分）
```

### ConVar 设置

```bash
sm_cvar sm_asrd_nuke_enabled 1        # 总开关 (0=关 1=开)
sm_cvar sm_asrd_nuke_damage 999999.0  # 对虫族伤害
sm_cvar sm_asrd_nuke_delay 3.0        # 倒计时秒数 (0=立即引爆)
sm_cvar sm_asrd_nuke_public 1         # 允许玩家用 sm_nukepub (0=仅管理员 1=所有人)
sm_cvar sm_asrd_nuke_debug 0          # 调试输出 (0/1)
```

---


## 四、范围击退（Repulse）v1.7.3

按一个键，以自己为中心震开周围所有虫族（可连按，默认无冷却）。击退采用每游戏帧平滑推进并附带速度，短时也不顿挫；内置完整虫族类名清单（含蚁狮 `npc_antlionguard` 系列、治疗虫 `asw_shaman`），并弹开敌方炮弹（mortarbug 炮弹/ ranger 酸球）。

**X-33 威力增强器护盾（v1.7.0 新增，v1.7.3 完善）：**

- **触发与限制**：仅重武兵 **Wildcat / Wolfe** 使用 `X-33 威力增强器`（buff 信标）时自动获得一段限时力场护盾，其他角色无护盾效果（他们也无法携带信标）。
- **充能计数**：由游戏原生管理——X-33 默认 5 次充能，每使用一次减 1，用完武器即销毁，永远开不了护盾。
- **叠加规则**：只叠时间不叠强度——2 秒内连用 3 次，护盾结束时间以**最后一次使用**为基准刷新。
- **护盾方式**：与手动护盾共用，仍由 `sm_asrd_repulse_aura_mode` 决定（默认 0 斥力弹开）；不依赖 `sm_asrd_repulse_aura`，也不需要 `sm_repulseaura` 指定。
- **屏幕倒计时**：护盾期间屏幕中央显示绿色「护盾 n 秒」倒计时，到期提示「X-33 力场护盾已失效」。
- **信标特效同步（v1.7.3）**：护盾期间每帧把原版信标的燃烧截止时间同步为护盾结束时间——**增益特效与武器伤害加成和倒计时同时开始/结束**，并抵消原版"携带信标移动每秒额外烧 1.125 秒"的消耗（只有重武兵能携带信标，故此前 Wildcat 的特效会提前熄灭）。
- **阵亡行为**：marine 阵亡护盾立即失效，但掉落的信标保留原版自然燃烧（队友路过半径内仍能蹭伤害增益）。

敌方投射物处理：**手动击退=弹开**；**护盾开启=进入半径的投射物直接消失**（`asw_mortarbug_shell` 炮虫炮弹例外，任何模式都只弹开不消失）。

> **关于墙体阻挡与流畅度（v1.6.0 行为）：**
>
> - 手动击退与护盾的位移都带碰撞检测，虫在推进途中**撞到墙体等障碍会被挡住停下，不再穿墙**（虫与虫、虫与玩家之间不互相阻挡，避免多只虫一起被推时误判堆叠；治疗虫 `asw_shaman`/`asw_mender` 在直接阻挡模式下仍严格钉在圈外）。
> - 护盾**直接阻挡**（mode 1）= 把进入半径的怪钉回半径边界，几乎无位移感。
> - 护盾**斥力弹开**（mode 0）= 给一个持续朝外的速度交给引擎物理连续推进，而不是分段动画，因此靠近护盾时不会一卡一卡；推速由 `sm_asrd_repulse_aura_push_speed` 调节（默认 900，越小越缓越柔和）。

### 玩家命令（默认公开）

```bash
sm_repulse        # 以自己为中心震开周围虫族
bind F9 "sm_repulse"      # 绑定示例（控制台输入一次，自动存 config.cfg）
```

### 管理员命令（仅 ADMFLAG_GENERIC，普通玩家不可用）

```bash
sm_repulseaura              # 切换自己的护盾
sm_repulseaura jack         # 切换 jack 的护盾
sm_repulseaura jack on      # 开启 jack 的护盾
sm_repulseaura jack off     # 关闭 jack 的护盾
sm_repulseaura #12345 on    # 用 userid 指定
```

管理员护盾**按玩家激活**：只有被 `sm_repulseaura` 指定的玩家身上才有效盾（总开关 `sm_asrd_repulse_aura` 默认 1 已开启，无需再手动开）。默认全场无护盾。这是独立于 X-33 护盾的特权通道。


### ConVar 设置

```bash
sm_cvar sm_asrd_repulse_enabled 1        # 总开关 (0=关 1=开)
sm_cvar sm_asrd_repulse_public 1         # 允许普通玩家使用 sm_repulse (0=仅管理员)
sm_cvar sm_asrd_repulse_radius 400       # 手动击退作用半径 (游戏单位)
sm_cvar sm_asrd_repulse_force 160        # 单次击退向外的总位移 (游戏单位)
sm_cvar sm_asrd_repulse_lift 80          # 单次击退向上挑飞高度
sm_cvar sm_asrd_repulse_pull_time 0.35   # 单次击退推进时长/秒 (越大越慢越平滑)
sm_cvar sm_asrd_repulse_cooldown 0       # 手动触发间隔秒 (0=无冷却可连按)

# 持续护盾 (管理员特权通道: 需 sm_repulseaura 指定到具体玩家才生效, 总开关默认已开)
sm_cvar sm_asrd_repulse_aura 1           # 护盾总开关 (0=关 1=开, 默认 1)
sm_cvar sm_asrd_repulse_aura_radius 260  # 护盾半径 (游戏单位)
sm_cvar sm_asrd_repulse_aura_mode 0      # 护盾模式 (0=斥力击退平滑弹开 / 1=直接阻挡钉在圈外)
sm_cvar sm_asrd_repulse_aura_push_speed 900  # 斥力模式把怪推出界外的速度 (单位/秒, 50~2000)

# X-33 威力增强器护盾: 仅 Wildcat/Wolfe(重武兵) 每使用一次 X-33
# (asw_weapon_buff_grenade, 默认5充能) 获得一段限时力场护盾
sm_cvar sm_asrd_repulse_x33 1            # X-33 护盾开关 (0=关 1=开; 不依赖 sm_asrd_repulse_aura)
sm_cvar sm_asrd_repulse_x33_duration 15  # 每用一次 X-33 的护盾秒数 — 同时控制护盾时长、
                                         # 屏幕倒计时、原版增益信标与武器伤害加成时长 (三者同步);
                                         # 设>15 等效于增强整个 X-33 道具 (默认 15)

# X-33 倒计时 HUD 显示 (改完立即生效, 无需重编译)
sm_cvar sm_asrd_repulse_x33_hud_channel 4   # 倒计时 HUD 通道 (避开核弹插件的 5)
sm_cvar sm_asrd_repulse_x33_hud_x -1.0      # 倒计时横向位置 (-1=居中 0=最左 0.6≈安全区最右;
                                            # 注意: 该游戏 HudText 只在屏幕左侧4:3区域渲染,
                                            # x≥0.7 的字会被裁掉完全看不见!)
sm_cvar sm_asrd_repulse_x33_hud_y 0.30      # 倒计时纵向位置 (0=最上 1=最下; 与核弹错开可改 0.20)

sm_cvar sm_asrd_repulse_classes ""        # 追加要击退的实体类名(空格分隔)，如 "npc_harvester asw_shaman"

# 敌方投射物(炮弹)弹开
sm_cvar sm_asrd_repulse_projectiles 1              # 是否弹开敌方炮弹 (0/1)
sm_cvar sm_asrd_repulse_projectile_speed 400        # 弹开速度(单位/秒)
sm_cvar sm_asrd_repulse_projectile_classes ""       # 追加要弹开的投射物类名(空格分隔)，如 "asw_mortarbug_shell"

sm_cvar sm_asrd_repulse_debug 0          # 调试输出 (0/1; 1 会列出半径内所有实体的真实类名,
                                          #       X-33 事件会打印职业判定与 HUD 模式)
```

> **如果某只虫/炮弹推不动：** 先开 debug（`sm_cvar sm_asrd_repulse_debug 1`）按时，服务器控制台会打印半径内所有实体的真实类名：
>
> - 把"笨虫"的类名填进 `sm_asrd_repulse_classes`
> - 把炮弹类名(如 ranger 酸液)填进 `sm_asrd_repulse_projectile_classes`  
    >   无需改代码重编译。已内置：蚁狮守卫/工蜂(`npc_antlionguard*`/`npc_antlion_worker`)、做成炮弹(`asw_mortarbug_shell`)、ranger 酸球(`asw_missile_round`，只弹敌方，不误伤玩家火箭)。

---

## 五、电锯高速旋转（Chainsaw Turbo）v1.2.0

手持电锯按住攻击键时锯片转得更快（被动生效，无需命令触发）。

### 管理员命令

```bash
sm_chainsaw_status   # 查看当前谁手持电锯、是否开火等状态
```

### ConVar 设置

```bash
sm_cvar sm_asrd_chainsaw_enabled 1    # 总开关 (0=关 1=开)
sm_cvar sm_asrd_chainsaw_speed 3.0    # 锯片转速倍率 (1.0~12.0, 3.0=快3倍)
sm_cvar sm_asrd_chainsaw_public 0     # 对普通玩家生效 (0=仅管理员 1=所有人)
sm_cvar sm_asrd_chainsaw_debug 0      # 调试输出 (0/1)
```

---

## 六、叛变虫群（Alien Civil War）v4.7.0

生成一批攻击虫族（而非玩家）的叛变虫，外观着色区分（默认绿色），可自选虫种、免冷却。

### 管理员命令

```bash
sm_betray drone 10      # 生成叛变虫（type=虫种, count=数量, 默认10）
sm_betray drone 10 jack # 生成到玩家 jack 身旁（可选第3参数 target, 可填名字/部分名/#userid）
sm_betray_list          # 列出可生成的虫种
sm_betray_clear         # 清除本插件生成的所有叛变虫
```

### 玩家命令

```bash
sm_betraypub drone 10   # 生成叛变虫（需 betray_public=1，默认开；聊天框 /buy 2 同样触发并扣积分）
bind F8 "sm_betraypub"  # 绑定示例
```


### ConVar 设置

```bash
sm_cvar sm_asrd_betray_enabled 1        # 总开关 (0=关 1=开)
sm_cvar sm_asrd_betray_type asw_drone   # 默认生成的虫种类名
sm_cvar sm_asrd_betray_count 10         # 默认每批生成数量 (上限 80)
sm_cvar sm_asrd_betray_spread 120.0     # 生成散布半径
sm_cvar sm_asrd_betray_color "37 217 73" # 叛变虫着色 RGB（默认绿色）
sm_cvar sm_asrd_betray_public 1         # 允许玩家用 sm_betraypub (0=仅管理员 1=所有人, 默认开)
sm_cvar sm_asrd_betray_drone_scale 1.0  # 叛变 drone 体型缩放倍率（仅 asw_drone, 1.0=默认）
sm_cvar sm_asrd_betray_damage_mult 4.0  # 叛变虫攻击力倍率（仅叛变虫造成的伤害, 1.0=不增强）
sm_cvar sm_asrd_betray_health_mult 5.0  # 叛变虫血量倍率（仅叛变虫, 默认 5 倍）
sm_cvar sm_asrd_betray_speed_mult 2.0   # 叛变虫移动速度倍率（仅叛变虫, 1.0=默认速度）
sm_cvar sm_asrd_betray_anim_mult 2.0    # 叛变虫动画速度倍率（仅叛变虫, 1.0=默认动画速度）
sm_cvar sm_asrd_betray_debug 0          # 调试输出 (0/1)
sm_cvar sm_asrd_betray_heal 1           # 叛变治疗虫(asw_shaman)给附近受伤人族玩家加血 (0=关 1=开)
sm_cvar sm_asrd_betray_heal_radius 400.0 # 治疗虫加血作用半径 (游戏单位)
sm_cvar sm_asrd_betray_heal_percent 0.08 # 治疗虫每次判定恢复目标最大血量的比例 (0.08=8%)
sm_cvar sm_asrd_betray_heal_tick 0.5    # 治疗虫加血判定周期 (秒, >=0.1)
```


### 可生成虫种（sm_betray 的 type 参数）

| 类名                        | 中文名       | 别名             |
| ------------------------- | --------- | -------------- |
| `asw_drone`               | 普通工蜂      | `drone`        |
| `asw_drone_jumper`        | 跳跃工蜂      | `jumper`       |
| `asw_buzzer`              | 蜂群        | `buzzer`       |
| `asw_parasite`            | 抱脸寄生虫     | `parasite`     |
| `asw_parasite_defanged`   | 拔牙寄生虫     | `defanged`     |
| `asw_boomer`              | 爆裂虫       | `boomer`       |
| `asw_ranger`              | 游侠        | `ranger`       |
| `asw_shieldbug`           | 盾甲虫       | `shield`       |
| `asw_mortarbug`           | 迫击炮虫      | `mortar`       |
| `asw_harvester`           | 收割者       | `harvester`    |
| `asw_grub`                | 幼虫        | `grub`         |
| `asw_drone_antlion`       | 蚁狮工蜂      | `antonlion`    |
| `npc_antlion_worker`      | 蚁狮工蜂(npc) | `anlionwork`   |
| `npc_antlionguard`        | 蚁狮守卫      | `antlionguard` |
| `npc_antlionguard_cavern` | 蚁狮守卫(洞窟)  | `antlioncave`  |
| `npc_antlionguard_normal` | 蚁狮守卫(标准)  | `antlionnorm`  |
| `asw_shaman`              | 治疗虫       | `shaman`       |

> **治疗虫（`asw_shaman`）加血说明：** 叛变治疗虫会周期性地给其作用半径内所有**血量不满的人族玩家**回血（默认每次恢复最大血量的 8%，每 0.5 秒判定一次）。该效果由插件独立实现、不依赖原生 AI 治疗，保证生效；可用 `sm_asrd_betray_heal=0` 关闭。
>
> **蚁狮说明：** AS:RD 中蚁狮守卫/工蜂真实类名为 `npc_` 前缀。若该类名在当前版本不可正常创建实体（`CreateEntityByName` 返回失败），会打印调用日志并跳过，不影响其他虫种；建议实测后确认能否正常生成及转成友军。`asw_drone_antlion`（蚁狮工蜂）已确认存在。

---


## 七、积分机制（Points）v1.9.4

全队共享一套积分经济：击杀虫族得分（按被杀虫族最大血量 × `hp_scale`，最少 1 分）、积分购买核弹 / 叛变虫群 / 强化等级。**积分购买唯一入口是聊天框**，控制台没有任何 `sm_points*` 命令。

积分池规则：

- 全队共享一个积分池：任何玩家击杀都加分，任何玩家都能消费。
- 每局（开局 / 换图 / 任务重开 / `restart`）重置为初始积分 `sm_asrd_points_start`（默认 1000）。
- 管理员与普通玩家一致，使用玩家命令同样扣分；管理员命令（`sm_nuke` / `sm_betray` / `sm_power_set`）不拦截、不扣积分。
- 观战 / 等待 / 未控制陆战队员的玩家不能消费积分（提示"请先加入游戏"）。
- 击杀**友军虫**（叛变虫群，含玩家附身的虫）不计分，防刷分；核弹 / 哨戒塔 / 扫描机 / 虫族互杀的击杀不计分。

### 玩家命令（聊天框输入，唯一扣积分入口）

```bash
/buy              # 显示购买格式与价格（附当前总积分）
/buy 1            # 购买战术核弹（扣 nuke_cost，默认 100 分）
/buy 2 [1-6]      # 购买友军虫群（扣 betray_cost，默认 100 分）
                  #   1=工蜂×10 2=蜂群×20 3=游侠×10 4=盾甲虫×5 5=迫击炮虫×10 6=治疗虫×5
                  #   （无选项 = 工蜂×10）
/buy 3            # 属性强化 +1（扣 power_cost，默认 200 分）
                  #   强化满级后再购买：血量<800 花 power_cost 恢复 200 血
                  #   （封顶最大血量 1000）；血量≥800 或积分不足则仅提示不扣分
/1                # 快捷：购买战术核弹
/2                # 快捷：购买友军虫群（工蜂×10）
/3                # 快捷：属性强化 +1（满级后同 /buy 3 转为加血）
/nukepub          # 直接调原功能命令同样扣积分（等价 /buy 1）
/betraypub [虫种] [数量]  # 直接调原功能命令同样扣积分（打错虫种名不会白扣）
/power_up         # 直接调原功能命令同样扣积分（等价 /buy 3）
/power_down       # 缩小体型（同样扣 power_cost）
/power_reset      # 恢复默认强化（免费，不扣积分）
```

> 注：价格设为 0 = 该功能不设积分门槛，维持原插件自己的开关行为。购买前会先校验原插件的总开关与 public 开关，未开放 / 未加载不会扣分；积分不足会提示且不扣分。

### ConVar 设置

```bash
sm_cvar sm_asrd_points_enabled 1          # 总开关 (0=关 1=开)
sm_cvar sm_asrd_points_start 1000         # 每局 (开局/换图/重开) 初始积分
sm_cvar sm_asrd_points_hp_scale 0.05      # 击杀积分 = 虫族最大血量 x 倍率 (最少 1 分)
sm_cvar sm_asrd_points_nuke_cost 100      # 核弹价格 (0=不设门槛)
sm_cvar sm_asrd_points_betray_cost 100    # 叛变虫群价格 (0=不设门槛)
sm_cvar sm_asrd_points_power_cost 200     # 强化等级价格 (0=不设门槛; 恢复默认免费)

# 积分 HUD (屏幕左上角常驻, 含积分为 0 时也显示; 半透明字体)
sm_cvar sm_asrd_points_hud 1              # 积分显示开关 (0=关 1=开)
sm_cvar sm_asrd_points_hud_channel 6      # HUD 通道 (避开 4=哨戒塔/X-33, 5=核弹)
sm_cvar sm_asrd_points_hud_x 0.01         # 横向位置 (0=最左, -1=居中; 该游戏 HudText 只渲染左侧 4:3 区域)
sm_cvar sm_asrd_points_hud_y 0.02         # 纵向位置 (0=最上 1=最下)
sm_cvar sm_asrd_points_hud_alpha 170      # 文字透明度 (0=全透明 255=不透明, 170≈半透明)

# 聊天使用说明公告 (帧回调驱动, 每 30 秒循环)
sm_cvar sm_asrd_points_advert 1           # 公告开关 (0=关 1=开)
sm_cvar sm_asrd_points_advert_interval 30 # 公告间隔秒数 (>=5)

sm_cvar sm_asrd_points_debug 0            # 调试输出 (0=关 1=开)
```

### 与其它插件的联动

| 购买项 | 聊天命令 | 转发给 | 前置条件 |
| ---- | ---- | ---- | ---- |
| 战术核弹 | `/buy 1` `/1` | `sm_nukepub` | 核弹插件已加载且 `sm_asrd_nuke_public 1` |
| 友军虫群 | `/buy 2 [1-6]` `/2` | `sm_betraypub` | 叛变虫群插件已加载且 `sm_asrd_betray_public 1` |
| 属性强化 | `/buy 3` `/3` | `sm_power_up` | 强化插件已加载且 `sm_asrd_power_public 1` |

---

## 八、一键开锁（Auto Unlock）v1.0.3

破解游戏内门锁 mini-game（连线拼图/转盘破解），直接解锁上锁的按钮区（大门）。

### 玩家命令（默认公开）

```bash
sm_unlock                # 解锁以你为中心 unlock_range 内最近的一扇上锁门锁
bind F9 "sm_unlock"      # 绑定示例
```

> `sm_unlock` 会解锁以你为中心 `unlock_range`（默认 150 单位，很小）内最近的一扇上锁门锁，必须贴近门才生效，不做大范围自动解锁。

### 管理员命令

```bash
sm_unlock_all            # 解锁整张地图所有上锁按钮区
sm_unlock_status         # 列出场上按钮区锁/电状态（排查用）
```

### ConVar 设置

```bash
sm_cvar sm_asrd_unlock_enabled 1     # 总开关 (0=关 1=开)
sm_cvar sm_asrd_unlock_public 1      # 允许普通玩家用 sm_unlock (0=仅管理员 1=公开)
sm_cvar sm_asrd_unlock_range 150     # sm_unlock 附近解锁范围（世界单位，需贴近门锁才能解，0=不限）
sm_cvar sm_asrd_unlock_forcepower 1  # 断电的门先送 PowerOn 再解锁 (0=只解锁)
sm_cvar sm_asrd_unlock_debug 0       # 调试输出 (0=关 1=开)
```

### 原理与局限

- 门锁实体为 `trigger_asw_button_area`（引擎类 `CASW_Button_Area`），破解完成后引擎 `InputUnlock` 会把 `m_bIsLocked` 置 false、`m_fHackProgress` 置 1.0 来开门。本插件直接向目标发送引擎输入 `"Unlock"` 触发同一逻辑，从而绕过连线/转盘小游戏。
- 个别地图若把门开的剧情绑定在 `OnButtonHackCompleted` 输出点（而非仅依赖锁状态/`useafterhack`），`InputUnlock` 不会触发该输出点，门可能开了但剧情不推进——此时请改用 `sm_unlock_all` 或先测试确认。

---

## 九、战斗扫描机召唤（Scanner）v1.3.3

在目标玩家身边召唤战斗扫描机（HL2 联合军城市扫描机），扫描机加入 marine 阵营：主动俯冲攻击虫族、不攻击玩家、**多台之间互不攻击**（已从引擎源码定位互攻根因并修复）。

### 管理员命令

```bash
sm_scanner            # 给自己召唤 1 台（数量走 sm_asrd_scanner_count）
sm_scanner 3          # 给自己召唤 3 台
sm_scanner jack       # 召唤到玩家 jack 身旁
sm_scanner 3 jack     # 召唤 3 台到 jack 身旁
sm_scanner_clear      # 清除本插件召唤的所有扫描机
sm_scanner_diag       # 召唤失败自检：实体能否创建 / 资源是否齐全 / 阵营真值 / 空中节点
sm_scanner_tp         # 把本插件所有扫描机传送到自己头顶（验证它们是否还在场上）
```

### 玩家命令

```bash
sm_scannerpub 3       # 为自己召唤 3 台（需 scanner_public=1，默认关）
```

### ConVar 设置

```bash
sm_cvar sm_asrd_scanner_enabled 1        # 总开关 (0=关 1=开)
sm_cvar sm_asrd_scanner_count 1          # 每次召唤数量 (1~10)
sm_cvar sm_asrd_scanner_health 20000     # 扫描机血量 (0=保持引擎默认 30；asrd_drone.sp 实测 20000)
sm_cvar sm_asrd_scanner_damage_mult 1.0  # 攻击力倍率 (俯冲伤害原生 25, 2.0=50)
sm_cvar sm_asrd_scanner_limit 10         # 场上扫描机上限 (0=不限)
sm_cvar sm_asrd_scanner_spread 40        # 多机横向错开间距 (drone 原版=40)
sm_cvar sm_asrd_scanner_public 0         # 允许玩家用 sm_scannerpub (0=仅管理员 1=所有人)
sm_cvar sm_asrd_scanner_debug 0          # 调试输出 (0=关 1=开)

# v1.2.0 新增（存活相关，physdamage 是能不能刷出来的关键）
sm_cvar sm_asrd_scanner_physdamage 0.0   # 物理撞击伤害倍率，0=免疫（见下方"根因"）
sm_cvar sm_asrd_scanner_neutral 0        # 中立模式：引擎设 DAMAGE_NO 无敌，但不主动攻击
sm_cvar sm_asrd_scanner_follow 1         # 只把玩家当观察目标 (m_bOnlyInspectPlayers)
sm_cvar sm_asrd_scanner_seek 0           # 生成后锁定最近的虫族（会高速俯冲自撞，默认关）
sm_cvar sm_asrd_scanner_turret 0         # 挂载哨戒机枪/加农炮塔（复刻 asrd_drone.sp 的火力，建议开）

# v1.3.0 新增
sm_cvar sm_asrd_scanner_fx 1             # 挂粒子特效+动态聚光灯（drone 原版配置，保证肉眼可见）
sm_cvar sm_asrd_scanner_marinefaction 1  # 写入 marine 阵营（0=完全走 drone 原版，不改阵营）

# v1.3.2 新增（解决"生成后慢慢往上飘走"）
sm_cvar sm_asrd_scanner_followmode 1     # 引擎原生跟随（m_nFlyMode=FOLLOW + 每秒刷新 m_vInspectPos）
sm_cvar sm_asrd_scanner_height 100       # 跟随时的悬停高度（相对队员原点）
```

---

## 十、挂机检测与踢出（Auto-AFK）v1.0.0

检测长时间无任何操作（键盘 / 鼠标）的玩家，超时后自动踢出；超时时间可动态调整。

**判定依据**：
- **主线**：每游戏帧（`OnGameFrame`）比较每个玩家的**视角角度**（`GetClientEyeAngles`）+ **绝对位置**（`GetClientAbsOrigin`）。动鼠标转视角 → 角度变；游戏内走路/被传送 → 位置变。任一路有变化即判定为活动。
- **鼠标点击**：监听官方游戏事件 `player_shoot` / `player_alt_fire` / `weapon_fire`，原地点鼠标开火也能刷新计时。
- **辅助**：保留 `OnPlayerRunCmd` 读按键/鼠标/使用，能触发时作为原地按键等场景的补充通道。

AS:RD 的玩家输入不依赖标准 `OnPlayerRunCmd`，因此上述主线程为可靠判定方式，同时覆盖**游玩中**与**观战/等待**状态（观战无可控实体，靠视角角度变化判定）。超时时间内以上均无变化即判定挂机。

**提醒与处理流程**：进入踢出前警告窗口（默认超时前 20 秒）后，**每秒**向该挂机玩家本人聊天栏提醒一次；只要玩家动了鼠标或键盘即刷新计时、离开警告窗口并停止提醒；超时后自动踢出（管理员默认不豁免，`sm_asrd_afk_adm_exempt 0`）。

### 玩家命令（默认公开）

```bash
sm_afk            # 查看当前超时设置（超时秒数/分钟、警告提前秒数、总开关状态）
```

### ConVar 设置

```bash
sm_cvar sm_asrd_afk_enabled 1     # 总开关 (0=关 1=开)
sm_cvar sm_asrd_afk_timeout 300   # 挂机超时秒数（默认 300 = 5 分钟；改 600 即 10 分钟）
sm_cvar sm_asrd_afk_interval 1.0  # 检查扫描间隔秒数（默认 1.0 → 警告窗口内等效每秒提醒）
sm_cvar sm_asrd_afk_warn 20       # 踢出前提前多少秒进入警告窗口，窗口内每秒提醒 (0=不提醒)
sm_cvar sm_asrd_afk_adm_exempt 0  # 是否豁免管理员 (0=不豁免 1=豁免, 默认 0 不豁免)
sm_cvar sm_asrd_afk_version       # 插件版本号（查询）
```
