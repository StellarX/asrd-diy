# AS:RD 插件集命令总览

> 项目：`D:\project\asrddiy` ｜ 游戏：Alien Swarm: Reactive Drop (AppID 563560)
> 共 7 个插件，按插件分节。所有管理员命令需 `ADMFLAG_GENERIC`（通用管理员权限）。

## 一、总览表

| 插件 | 源文件 | 版本 | 玩家命令默认公开？ |
|------|--------|------|:---:|
| 陆战队员强化 | `asrd_marine_power.sp` | 1.2.0 | ✅ 公开 |
| 哨戒塔增强 + 头顶塔 + HUD | `asrd_sentry_enhancer.sp` | 6.3.0 | 头顶塔 ✅ / 掉落 ❌ |
| 核弹轰炸 | `asrd_nuke.sp` | 1.7.1 | ❌ 仅管理员 |
| 实体趣味操控 | `asrd_entity_fun.sp` | 1.0.0 | ❌ 全管理员 |
| 无人机随从 | `asrd_drone_escort.sp` | 1.0.0 | ✅ 公开 |
| 电锯高速旋转 | `asrd_chainsaw_turbo.sp` | 1.2.0 | ❌ 仅管理员（被动生效） |
| 叛变虫群 | `asrd_alien_civilwar.sp` | 3.0.0 | ❌ 仅管理员 |

---

## 二、陆战队员强化（Marine Power）

按键实时调大/调小自己的血量、体型、近战、移速。

### 普通玩家命令（默认公开，`sm_asrd_power_public` 默认 1）

| 命令 | 说明 |
|------|------|
| `sm_power_up` | 调大 1 级（血量 +400/级、体型 +0.2/级、移速同步、近战全局放大） |
| `sm_power_down` | 调小 1 级（仅模型缩小 0.8/0.6/0.4 倍） |
| `sm_power_reset` | 恢复默认（等级 0 = 100 血 / 1.0 体型 / 1.0 移速） |

> 绑定示例：`bind F6 "sm_power_up"` ｜ `bind F7 "sm_power_down"`

### 管理员命令

| 命令 | 说明 |
|------|------|
| `sm_power_status` | 查看所有玩家强化状态 |

---

## 三、哨戒塔增强 + 头顶哨戒塔 + 信息 HUD（Sentry Enhancer）

增强地图哨戒塔（生命/射速/射程/弹药/伤害），支持把塔放头顶当"随行炮台"。

### 管理员命令

| 命令 | 说明 |
|------|------|
| `sm_sentry_refresh` | 重新增强所有哨戒塔并补满弹药 |
| `sm_sentry_status` | 查看所有哨戒塔状态 |
| `sm_sentry_boost` | 一键满配增强所有哨戒塔 |
| `sm_sentry_unboost` | 一键还原增强倍率到默认档 |
| `sm_sentryhat [yaw]` | 把最近的塔放到自己头顶（`yaw` 为朝向偏移度数） |
| `sm_sentryhat_off` | 取消所有玩家的头顶塔 |
| `sm_sentry_drop [type]` | 在身边掉落哨戒塔拾取箱（`type`：0 机枪 / 1 炮 / 2 喷火 / 3 冰冻，默认炮） |
| `sm_sentry_dump` | 转储哨戒塔内部属性（调试） |
| `sm_sentry_dump_player` | 转储玩家实体属性（调试） |

### 普通玩家命令

| 命令 | 说明 | 默认可用？ |
|------|------|:---:|
| `sm_hat [yaw]` | 把最近的塔放到自己头顶 | ✅（`sm_asrd_sentry_hat_public`=1） |
| `sm_hat_off` | 取消自己的头顶塔 | ✅ |
| `sm_sentryhud` | 开关右上角哨戒塔信息 HUD | ✅ |
| `sm_sentrydrop [type]` | 在身边掉落哨戒塔拾取箱 | ❌（`sm_asrd_sentry_drop_public`=0） |

> 放头顶需离塔足够近（`sm_asrd_sentry_hat_maxdist` 默认 100，0=不限距离）。

---

## 四、核弹轰炸（Nuke Strike）

倒计时后全图清除虫族（含异形卵）。

### 管理员命令

| 命令 | 说明 |
|------|------|
| `sm_nuke` | 呼叫战术核弹轰炸全图虫族 |

### 普通玩家命令

| 命令 | 说明 | 默认可用？ |
|------|------|:---:|
| `sm_nukepub` | 呼叫战术核弹 | ❌（`sm_asrd_nuke_public`=0） |

> 全局单发锁：倒计时（默认 5 秒）结束前，任何人再呼叫都会被拒。

---

## 五、实体趣味操控（Entity Fun）

对实体做传送、父子绑定、穿墙、冻结、弹射等操控（演示性质）。

### 管理员命令（全部仅管理员，无玩家命令）

| 命令 | 说明 |
|------|------|
| `sm_sentryhat [target]` | 把机枪塔放到目标玩家头顶 |
| `sm_sentryhat_off` | 取消所有头顶机枪塔 |
| `sm_stack <target1> <target2>` | 把 target1 叠到 target2 头顶 |
| `sm_noclip_marine [target]` | 让目标陆战队员穿墙飞行 |
| `sm_freeze_marine [target]` | 冻结/解冻目标陆战队员 |
| `sm_launch [target]` | 把目标向上弹射 |
| `sm_slam [target]` | 把目标砸向地面 |

---

## 六、无人机随从（Drone Escort）

按一个键生成自动环绕跟随、攻击异形的机器无人机。

### 普通玩家命令（默认公开，`sm_asrd_drone_public` 默认 1）

| 命令 | 说明 |
|------|------|
| `sm_drone` | 生成 1 架随从无人机（每名玩家最多 `sm_asrd_drone_limit` 架，默认 10） |

> 绑定示例：`bind F8 "sm_drone"`

### 管理员命令

| 命令 | 说明 |
|------|------|
| `sm_drone_clear [玩家名]` | 清空无人机（不填则清全部，可填玩家名） |
| `sm_drone_status` | 查看每名玩家的无人机数量 |

---

## 七、电锯高速旋转（Chainsaw Turbo）

手持电锯按住攻击键时让锯片转得更快（被动生效，无需主动命令触发）。

### 管理员命令

| 命令 | 说明 |
|------|------|
| `sm_chainsaw_status` | 查看当前谁手持电锯、是否开火等状态 |

### 普通玩家命令

无（被动生效，`sm_asrd_chainsaw_public` 默认 0 = 仅管理员生效；设 1 则所有人）。

---

## 八、叛变虫群（Alien Civil War）

生成一批攻击**虫族**（而非玩家）的叛变虫，外观着色区分，免冷却。

### 管理员命令

| 命令 | 说明 |
|------|------|
| `sm_betray [type] [count]` | 生成叛变虫群（`type` 虫种，`count` 数量，默认 10） |
| `sm_betray_list` | 列出可生成的虫种 |
| `sm_betray_clear` | 清除本插件生成的所有叛变虫 |

### 普通玩家命令

| 命令 | 说明 | 默认可用？ |
|------|------|:---:|
| `sm_betraypub [type] [count]` | 生成叛变虫群 | ❌（`sm_asrd_betray_public`=0） |

### 可生成虫种（type 参数）

`drone` 普通工蜂、`jumper` 跳跃工蜂、`buzzer` 蜂群、`parasite` 抱脸寄生虫、
`defanged` 拔牙寄生虫、`boomer` 爆裂虫、`ranger` 游侠、`shield` 盾甲虫、
`mortar` 迫击炮虫、`harvester` 收割者、`grub` 幼虫。
